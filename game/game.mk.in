# Variables
@liba = glad

# Include
@%mk/cbin.mk.in

# Rules
$(@outfile): LDLIBS += -lglfw
$(@obj): CFLAGS += -Wpedantic -Werror -Wfatal-errors

# macOS
$(@obj) $($outfile): LDFLAGS += -framework Cocoa -framework OpenGL -framework IOKit -framework CoreVideo
