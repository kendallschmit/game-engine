engine.h
    defines
        vectors
        timing
    kge_obj
        add, remove
        two groups
            physical
            non-physical
    load texture
engine.c
    kge_thread
    render_thread
        draw utils
        vao util
        textures
    input/main_thread (also creates / owns window)

